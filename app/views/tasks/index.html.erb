<%# Workflow 1 · Build — designed → building → submitted → reviewed (+ archived). %>
<%= render "board",
      stages: Task::TASKS_BOARD_STAGES,
      title: "Tasks",
      archived: true,
      current_page: "tasks" %>
