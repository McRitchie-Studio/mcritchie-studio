<%# Workflow 1 · Build (feature agent) — designed → building → blocked → submitted (+ archived toggle). %>
<%= render "board",
      stages: Task::TASKS_BOARD_STAGES,
      title: "Tasks",
      archived: true,
      current_page: "tasks" %>
